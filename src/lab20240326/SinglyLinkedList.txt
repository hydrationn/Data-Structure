» 교재의 코드들을 이용하여 단순 연결 리스트 클래스를 완성한다.

1. insertLast(x) 메소드 구현하기
: 리스트의 맨 마지막에 원소를 삽입하는 메소드

2. printall() 메소드 구현하기
: 삽입이 끝난 완성된 리스트를 출력하는 메소드
-> 입력 파일을 한 줄씩 읽어 리스트에 차례대로 삽입한 후 리스트를 출력한다.

3. reverse() 메소드 구현하기
: 참조 변수들을 이용하여 역방향 연결 리스트로 변환하는 메소드
- 단, 새로운 node 및 LinkedList 생성 X
- 기존 node들의 참조 변수를 수정해서 완성
